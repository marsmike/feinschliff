---
extends: feinschliff
version: alpha
name: Claude
description: Claude — warm cream canvas + coral accent + serif editorial register.
colors:
  accent: "#cc785c"  # Coral — the Anthropic warm coral, primary CTA fill.
  accent-hover: "#a9583e"  # Coral pressed / active.
  highlight: "#e8a55a"  # Companion warm amber — small inline highlights, badges.
  black: "#141413"  # Ink — warm dark, slightly off-pure-black.
  ink: "#141413"  # Body ink on cream = same warm dark.
  graphite: "#252523"  # Body strong — emphasized paragraphs, lead text.
  steel: "#6c6a64"  # Muted — sub-headings, breadcrumbs, secondary text.
  silver: "#8e8b82"  # Muted soft — captions, fine-print.
  fog: "#e6dfd8"  # Hairline border on cream surfaces.
  paper: "#faf9f5"  # Canvas — tinted cream, the brand-defining warm white.
  paper-2: "#efe9de"  # Surface card — feature cards, content cards.
  off-white: "#faf9f5"  # On-dark text — cream-tinted white that echoes the canvas.
  off-white-2: "#a09d96"  # On-dark soft — footer body text, secondary labels.
  rule-dark: "#252320"  # Hairlines on dark surfaces.
  surface-dark: "#181715"  # Code editors, model showcase cards, footer.
  surface-dark-elevated: "#252320"  # Elevated cards inside dark bands.
  surface-dark-soft: "#1f1e1b"  # Code block backgrounds inside dark cards.
  surface-soft: "#f5f0e8"  # Section dividers, soft band backgrounds.
  surface-cream-strong: "#e8e0d2"  # Strongest cream — selected tabs, emphasized bands.
  accent-teal: "#5db8a6"  # Secondary product accent — status dots, terminal indicators.
  success: "#5db872"  # Status success.
  warning: "#d4a017"  # Status warning.
  error: "#c64545"  # Status error.
  white: "#ffffff"
---

## Overview

Claude — warm cream canvas + coral accent + serif editorial register. Derived from claude/DESIGN.md (Anthropic Claude product surface). MIT.

## Typography

Defined in tokens.json (`font-family`, `font-weight`, `font-size`).
Inherits from this brand's tokens for now; future revisions may move
typography tokens into DESIGN.md frontmatter.
