"""Verify subpackage: deterministic and LLM-backed defect scanners.

`chrome.py` runs deterministic Layer 1 chrome checks.
`deck/` ships deterministic helpers used by Layer 3 LLM rubrics.
`llm/rubric.py` runs the executable LLM rubric and emits Defect records.
"""
