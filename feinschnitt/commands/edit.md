---
name: edit
description: "Edit a pre-recorded video — brand-themed overlays, zoom punch-ins, verified render. Usage: /edit <video> [notes]"
user_invocable: true
---

Engage the `edit` skill (`skills/edit/SKILL.md`) for: $ARGUMENTS

Follow the skill's workflow exactly: probe → transcribe → author plan →
lint → preview render → STOP for user approval → final render.
